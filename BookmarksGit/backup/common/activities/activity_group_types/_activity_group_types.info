# Name of group, localization is selected by: "activity_group_type" + <key>
key = {
	sort_order = integer			# The order activity groups show up in the activity view, higher number is sorted first, tie breaking is on definition order. Default = 0
	gui_tags = { tag_1 tag_2 ... }	# List of gui tags, used to set size etc in gui views
}
