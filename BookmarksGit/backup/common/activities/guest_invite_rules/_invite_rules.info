# Scriptable list of guests for a given category
friends = {
	# Build the list of possible characters
	#
	# root = character hosting the activity
	# scope:special_option = flag of the selected special option, not set if activity has no special option category
	# scope:<option_category_key> = for every option category defined, will have a flag of the selected option
	#
	# use add_to_list = characters to save all characters who are valid for this guest invite list
	effect = {
		every_relation = {
			type = friend
			add_to_list = characters
		}
	}
}

Localization:
key: "Name of list"
key_desc: "Description of this list"
